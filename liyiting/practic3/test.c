#include "link.h"
#include "randArr.h"

int main(int argc,char **argv){

	int arr[10];
	outArr(arr,10);
	void insertSort(arr,n);
	outArr(arr,n);
	void bubbleSort(arr,n);
        outArr(arr,n);
	void selectSort(arr,n);
        outArr(arr,n);
	void shellSort(arr,n);
        outArr(arr,n);
}

