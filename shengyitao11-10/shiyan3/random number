#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#define N 20

int main (int argc,char **argv){
    int a[N], i;
    srand((int)time(NULL));
    for(i=0;i<N;i++)
        a[i]=rand()%100;
     printf("origin data:\n");
     for(i=0;i<N;i++){
        printf("%d\t",a[i]);
       }
       return 0;
  }
