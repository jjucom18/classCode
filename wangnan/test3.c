#include<stdlib.h>
main void *malloc(size_t size);
int *ptr_ar;
int N;
printf("%d"," N:\n");
scanf("%d",&N);
if(N<=0),N=100;
ptr_ar=(int*);
malloc(sizeof(int)*N);
if(!ptr_ar)
{
	printf("malloc ptr_ar error !\n";
			exit(0);
		
}(else
pirntf("malloc ptr_ar success!\n");

